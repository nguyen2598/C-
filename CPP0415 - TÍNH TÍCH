#include<bits/stdc++.h>
using namespace std;

int main(){
int t;
cin>>t;
while(t--){
	long n,d;
	cin>>n>>d;
	long a[n],b[d];
	for(int i=0;i<n;i++)cin>>a[i];
	for(int i=0;i<d;i++)cin>>b[i];
	sort(a,a+n);
	sort(b,b+d);
	cout<<b[0]*a[n-1];
	cout<<endl;
}
}

